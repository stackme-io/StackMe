[build]
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "sh -c 'uv run uvicorn core.main:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"