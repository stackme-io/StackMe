[project]
name = "analyze-me"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.2",
    "numpy>=2.4.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["analyze_me"]